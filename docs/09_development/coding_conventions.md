# 코딩 규칙

- Python 타입 힌트를 사용한다.
- API 요청과 응답은 명시적인 스키마로 정의한다.
- 도메인 함수에 Streamlit 또는 공급자 SDK 객체를 전달하지 않는다.
- 외부 API 호출은 시간 초과를 지정한다.
- 로그에는 생성 실행 ID를 포함하되 비밀정보는 포함하지 않는다.
- 사용자에게 보여줄 오류와 내부 로그를 분리한다.
- 테스트가 어려운 전역 상태를 만들지 않는다.

