# 환경 설정

예상 환경변수:

```text
APP_ENV=
DATABASE_URL=
TEXT_AI_PROVIDER=
TEXT_AI_API_KEY=
IMAGE_AI_PROVIDER=
IMAGE_AI_API_KEY=
AI_REQUEST_TIMEOUT_SECONDS=
```

실제 변수명은 공급자 선정 후 확정한다. 예제 값만 포함하는 `.env.example`을
구현 단계에서 만들고 실제 키는 저장소에 추가하지 않는다.

