# 보안 정책

- API 키는 서버 환경변수 또는 비밀 저장소에서 관리한다.
- `.env` 파일은 버전 관리에서 제외한다.
- 외부 API 요청 로그에 사용자 입력 전체를 남기지 않는다.
- 서버는 허용된 파일 형식과 크기만 처리한다.
- 오류 응답에 스택 추적과 비밀정보를 포함하지 않는다.
- 외부 URL 및 파일 입력 기능 추가 시 별도 검증 정책을 정의한다.

